# ==================================================
# 公共超参数配置
# train.py 和 predict.py 都从这里读，改参数只改这一处
# ==================================================

# ---------- 模型结构 ----------
D_MODEL = 256        # 词向量维度
NUM_HEADS = 8        # 注意力头数（必须能整除 D_MODEL）
D_FF = 1024          # 前馈全连接层维度
NUM_LAYERS = 4       # 编码器 / 解码器层数
MAX_SEQ_LEN = 1024   # 最大序列长度（字符级分词 + 整句需要留足）

# ---------- 训练超参数 ----------
BATCH_SIZE = 4       # 每个 batch 的句对数
LR = 3e-4            # 学习率（模型变大后 1e-3 偏高，降到 3e-4）
EPOCHS = 2000        # 训练步数（每步取一个 batch）